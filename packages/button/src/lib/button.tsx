import ButtonMui, { ButtonProps } from '@mui/material/Button';

const Button = (props: ButtonProps) => <ButtonMui id="teste01" {...props} />;

export { Button };
export default Button;
export * from '@mui/material/Button';
