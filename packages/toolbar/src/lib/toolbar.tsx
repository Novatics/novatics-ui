import ToolbarMui, { ToolbarProps } from '@mui/material/Toolbar';

const Toolbar = (props: ToolbarProps) => <ToolbarMui {...props} />;

export { Toolbar };
export default Toolbar;
export * from '@mui/material/Toolbar';
