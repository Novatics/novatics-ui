import SkeletonMui, { SkeletonProps } from '@mui/material/Skeleton';

const Skeleton = (props: SkeletonProps) => <SkeletonMui {...props} />;

export { Skeleton };
export default Skeleton;
export * from '@mui/material/Skeleton';
