import CollapseMui, { CollapseProps } from '@mui/material/Collapse';

const Collapse = (props: CollapseProps) => <CollapseMui {...props} />;

export { Collapse };
export default Collapse;
export * from '@mui/material/Collapse';
