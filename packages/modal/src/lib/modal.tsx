import ModalMui, { ModalProps } from '@mui/material/Modal';

const Modal = (props: ModalProps) => <ModalMui {...props} />;

export { Modal };
export default Modal;
export * from '@mui/material/Modal';
