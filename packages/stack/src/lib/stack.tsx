import StackMui, { StackProps } from '@mui/material/Stack';

const Stack = (props: StackProps) => <StackMui {...props} />;

export { Stack };
export default Stack;
export * from '@mui/material/Stack';
