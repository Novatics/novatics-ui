import MenuListMui, { MenuListProps } from '@mui/material/MenuList';

const MenuList = (props: MenuListProps) => <MenuListMui {...props} />;

export { MenuList };
export default MenuList;
export * from '@mui/material/MenuList';
