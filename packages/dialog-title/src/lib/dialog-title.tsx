import DialogTitleMui, { DialogTitleProps } from '@mui/material/DialogTitle';

const DialogTitle = (props: DialogTitleProps) => <DialogTitleMui {...props} />;

export { DialogTitle };
export default DialogTitle;
export * from '@mui/material/DialogTitle';
