import FormLabelMui, { FormLabelProps } from '@mui/material/FormLabel';

const FormLabel = (props: FormLabelProps) => <FormLabelMui {...props} />;

export { FormLabel };
export default FormLabel;
export * from '@mui/material/FormLabel';
