import PortalMui, { PortalProps } from '@mui/material/Portal';

const Portal = (props: PortalProps) => <PortalMui {...props} />;

export { Portal };
export default Portal;
export * from '@mui/material/Portal';
