import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import Form from './form';
import FormikForm from './formik-form';
import pkg from '../../package.json';
import { Button } from '../../../button/src/index';
import { ActionFooter } from '../../../action-footer/src/index';
import { InputField, InputFormikField } from '../../../input-field/src/index';

<Meta title="Form" component={Form} />

# Form <img src={`https://img.shields.io/badge/Version-v${pkg.version}-brightgreenn`} />

### Installation

First install the latest version of the package from `@novatics/form`.

```shell
  yarn add -D @novatics/form
```

```shell
  npm install --dev @novatics/form
```

and use it

```tsx
import ... from '@novatics/form';

  ...


```

### Formik

<Canvas>
  <Story
    name="Forkmik"
    argTypes={{
      error: false,
      checked: false,
    }}
  >
    {(args) => {
      return (
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <FormikForm
            title={'Create a Project'}
            description={
              'Os projetos são onde seus repositórios vivem. Eles são contêineres nos quais você pode agrupar repositórios semelhantes para melhores organizações de código.'
            }
            onSubmit={(values) => {
              e.preventDefault();
              console.log('AUUUU', values);
            }}
            submitButtonProps={{
              children: 'create',
            }}
            initialValues={{ name: '', key: '' }}
            {...args}
          >
            {() => {
              return (
                <>
                  <InputFormikField id="name" name="name" required />
                  <InputFormikField id="key" name="key" />
                </>
              );
            }}
          </FormikForm>
        </div>
      );
    }}
  </Story>
</Canvas>

### Playground

<Canvas>
  <Story
    name="Playground"
    argTypes={{
      error: false,
      checked: false,
    }}
  >
    {(args) => {
      return (
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Form
            title={'Create a Project'}
            description={
              'Os projetos são onde seus repositórios vivem. Eles são contêineres nos quais você pode agrupar repositórios semelhantes para melhores organizações de código.'
            }
            handleSubmit={(e) => {
              e.preventDefault();
              console.log('AUUUU');
            }}
            submitButtonProps={{
              children: 'create',
            }}
            {...args}
          >
            <InputField
              label="Project's name"
              required
              hint="Some important text"
            />
            <InputField label="Key" required />
            <ActionFooter spacing={2}>
              <Button type="reset" variant="outlined">
                Reset
              </Button>
              <Button type="submit" variant="contained">
                Submit
              </Button>
            </ActionFooter>
          </Form>
        </div>
      );
    }}
  </Story>
</Canvas>

<ArgsTable story="Playground" />

made by Novatics ❤
