import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { ComponentStory, ComponentMeta } from '@storybook/react';
import pkg from '../../package.json';
import ChairOutlinedIcon from '@mui/icons-material/ChairOutlined';
import Chip from './chip';

<Meta title="Chip" component={Chip} />

# Chip <img src={`https://img.shields.io/badge/Version-v${pkg.version}-brightgreenn`} />

### Installation

First install the latest version of the package from `@novatics/chip`.

```shell
  yarn add -D @novatics/chip
```

### Basic Usage

<Canvas>
  <Story name="Basic Usage">
    {(args) => {
      const handleDelete = () => {};
      return (
        <div
          style={{
            display: 'flex',
            flexDirection: 'row',
            alignItems: 'center',
            gap: '10px',
          }}
        >
          <Chip
            label="Chip"
            size="medium"
            color="primary"
            icon={<ChairOutlinedIcon />}
            onDelete={handleDelete}
            // onClick={() => alert('clicked')}
            clickable
          />
          <Chip
            label="Chip"
            size="small"
            color="primary"
            icon={<ChairOutlinedIcon />}
            variant="outlined"
            onDelete={handleDelete}
            clickable
          />
        </div>
      );
    }}
  </Story>
</Canvas>

and use it

```tsx
import Chip from '@novatics/chip';

  ...
```

made by Novatics ❤
