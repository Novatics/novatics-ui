import ButtonMui, { ButtonProps } from '@mui/material/Button';

const Button = (props: ButtonProps) => <ButtonMui {...props}>Button</ButtonMui>;

export { Button };
export default Button;
export * from '@mui/material/Button';
