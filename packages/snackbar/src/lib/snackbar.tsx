import SnackbarMui, { SnackbarProps } from '@mui/material/Snackbar';

const Snackbar = (props: SnackbarProps) => <SnackbarMui {...props} />;

export { Snackbar };
export default Snackbar;
export * from '@mui/material/Snackbar';
