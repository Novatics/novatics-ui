import PopperMui, { PopperProps } from '@mui/material/Popper';

const Popper = (props: PopperProps) => <PopperMui {...props} />;

export { Popper };
export default Popper;
export * from '@mui/material/Popper';
