import InternalMui, { InternalProps } from '@mui/material/Internal';

const Internal = (props: InternalProps) => <InternalMui {...props} />;

export { Internal };
export default Internal;
export * from '@mui/material/Internal';
