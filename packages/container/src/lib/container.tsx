import ContainerMui, { ContainerProps } from '@mui/material/Container';

const Container = (props: ContainerProps) => <ContainerMui {...props} />;

export { Container };
export default Container;
export * from '@mui/material/Container';
