import ImageListItemBarMui, {
  ImageListItemBarProps,
} from '@mui/material/ImageListItemBar';

const ImageListItemBar = (props: ImageListItemBarProps) => (
  <ImageListItemBarMui {...props} />
);

export { ImageListItemBar };
export default ImageListItemBar;
export * from '@mui/material/ImageListItemBar';
