import TooltipMui, { TooltipProps } from '@mui/material/Tooltip';

const Tooltip = (props: TooltipProps) => <TooltipMui {...props} />;

export { Tooltip };
export default Tooltip;
export * from '@mui/material/Tooltip';
