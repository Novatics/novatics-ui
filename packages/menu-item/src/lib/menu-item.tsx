import MenuItemMui, { MenuItemProps } from '@mui/material/MenuItem';

const MenuItem = (props: MenuItemProps) => <MenuItemMui {...props} />;

export { MenuItem };
export default MenuItem;
export * from '@mui/material/MenuItem';
