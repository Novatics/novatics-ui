import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import pkg from '../../package.json';
import Radio from './radio';

<Meta title="Radio" component={Radio} />

# Radio <img src={`https://img.shields.io/badge/Version-v${pkg.version}-brightgreenn`} />

### Installation

First install the latest version of the package from `@novatics/radio`.

```shell
  yarn add -D @novatics/radio
```

### Dependencies

This component extends [Material UI React Radio](https://mui.com/material-ui/react-radio-button). Therefore is needed to install Material UI:

```bash
yarn add @mui/material
```

### Basic Usage

<Canvas>
  <Story name="Basic Usage">
    {(args) => {
      return (
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Radio {...args} />
        </div>
      );
    }}
  </Story>
</Canvas>

```tsx
import ... from '@novatics/radio';

  ...


```

### Variations

<Canvas>
  <Story name="Variations">
    <div
      style={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-around',
      }}
    >
      <div style={{ display: 'flex', flexDirection: 'column' }}>
        Default Behaviors
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Radio checked />
          <Radio />
        </div>
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Radio disabled checked />
          <Radio disabled />
        </div>
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Radio checked error />
          <Radio error />
        </div>
      </div>
      <div style={{ display: 'flex', flexDirection: 'column' }}>
        Color Variations
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Radio color="primary" checked />
          <Radio color="primary" />
        </div>
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Radio color="secondary" checked />
          <Radio color="secondary" />
        </div>
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Radio color="error" checked />
          <Radio color="error" />
        </div>
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Radio color="info" checked />
          <Radio color="info" />
        </div>
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Radio color="success" checked />
          <Radio color="success" />
        </div>
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Radio color="warning" checked />
          <Radio color="warning" />
        </div>
      </div>
    </div>
  </Story>
</Canvas>

### Playground

<Canvas>
  <Story
    name="Playground"
    argTypes={{
      error: false,
      checked: false,
    }}
  >
    {(args) => {
      return (
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Radio {...args} />
        </div>
      );
    }}
  </Story>
</Canvas>

<ArgsTable story="Playground" />

made by Novatics ❤
