import IconButtonMui, { IconButtonProps } from '@mui/material/IconButton';

const IconButton = (props: IconButtonProps) => <IconButtonMui {...props} />;

export { IconButton };
export default IconButton;
export * from '@mui/material/IconButton';
