import AlertTitleMui, { AlertTitleProps } from '@mui/material/AlertTitle';

const AlertTitle = (props: AlertTitleProps) => <AlertTitleMui {...props} />;

export { AlertTitle };
export default AlertTitle;
export * from '@mui/material/AlertTitle';
