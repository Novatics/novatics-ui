import PopoverMui, { PopoverProps } from '@mui/material/Popover';

const Popover = (props: PopoverProps) => <PopoverMui {...props} />;

export { Popover };
export default Popover;
export * from '@mui/material/Popover';
