import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { ComponentStory, ComponentMeta } from '@storybook/react';
import pkg from '../../package.json';
import Checkbox from './checkbox';

<Meta title="Checkbox" component={Checkbox} />

# Checkbox <img src={`https://img.shields.io/badge/Version-v${pkg.version}-brightgreenn`} />

### Installation

First install the latest version of the package from `@novatics/checkbox`.

```shell
  yarn add -D @novatics/checkbox
```


### Dependencies

This component extends [Material UI React Checkbox](https://mui.com/material-ui/react-checkbox-button). Therefore is needed to install Material UI:

```bash
yarn add @mui/material
```

### Basic Usage

<Canvas>
  <Story name="Basic Usage">
    {(args) => {
      return (
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Checkbox
            {...args}
          />
        </div>
      );
    }}
  </Story>
</Canvas>

```tsx
import ... from '@novatics/checkbox';

  ...


```

### Variations

<Canvas>
  <Story name="Variations">
    <div style={{ display: 'flex', flexDirection: 'row' }}>
      <Checkbox />
      <Checkbox checked />
      <Checkbox checked disabled />
      <Checkbox variant="outlined" />
      <Checkbox variant="outlined" checked />
      <Checkbox variant="outlined" checked disabled />
      <Checkbox indeterminate />
      <Checkbox indeterminate disabled />
      <Checkbox variant="outlined" indeterminate />
      <Checkbox variant="outlined" indeterminate disabled />
    </div>
  </Story>
</Canvas>

### Playground

<Canvas>
  <Story
    name="Playground"
    argTypes={{
      error: false,
      checked: false,
    }}
  >
    {(args) => {
      return (
        <div style={{ display: 'flex', flexDirection: 'row' }}>
          <Checkbox {...args} />
        </div>
      );
    }}
  </Story>
</Canvas>

<ArgsTable story="Playground" />


made by Novatics ❤
