import TransitionsMui, { TransitionsProps } from '@mui/material/Transitions';

const Transitions = (props: TransitionsProps) => <TransitionsMui {...props} />;

export { Transitions };
export default Transitions;
export * from '@mui/material/Transitions';
