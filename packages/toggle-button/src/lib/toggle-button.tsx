import ToggleButtonMui, { ToggleButtonProps } from '@mui/material/ToggleButton';

const ToggleButton = (props: ToggleButtonProps) => (
  <ToggleButtonMui {...props} />
);

export { ToggleButton };
export default ToggleButton;
export * from '@mui/material/ToggleButton';
