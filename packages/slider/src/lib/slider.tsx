import SliderMui, { SliderProps } from '@mui/material/Slider';

const Slider = (props: SliderProps) => <SliderMui {...props} />;

export { Slider };
export default Slider;
export * from '@mui/material/Slider';
