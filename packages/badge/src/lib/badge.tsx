import BadgeMui, { BadgeProps } from '@mui/material/Badge';

const Badge = (props: BadgeProps) => <BadgeMui {...props} />;

export { Badge };
export default Badge;
export * from '@mui/material/Badge';
