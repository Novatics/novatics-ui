import SelectMui, { SelectProps } from '@mui/material/Select';

const Select = (props: SelectProps) => <SelectMui {...props} />;

export { Select };
export default Select;
export * from '@mui/material/Select';
