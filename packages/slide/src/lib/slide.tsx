import SlideMui, { SlideProps } from '@mui/material/Slide';

const Slide = (props: SlideProps) => <SlideMui {...props} />;

export { Slide };
export default Slide;
export * from '@mui/material/Slide';
