import { ComponentStory, ComponentMeta } from '@storybook/react';

export default {
  title: 'Styles',
};

const Template = (args: unknown) => <div>Theme</div>;

export const Primary = Template.bind({});
