import FormGroupMui, { FormGroupProps } from '@mui/material/FormGroup';

const FormGroup = (props: FormGroupProps) => <FormGroupMui {...props} />;

export { FormGroup };
export default FormGroup;
export * from '@mui/material/FormGroup';
