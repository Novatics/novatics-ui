import DialogMui, { DialogProps } from '@mui/material/Dialog';

const Dialog = (props: DialogProps) => <DialogMui {...props} />;

export { Dialog };
export default Dialog;
export * from '@mui/material/Dialog';
