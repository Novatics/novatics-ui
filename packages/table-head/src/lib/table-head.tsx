import TableHeadMui, { TableHeadProps } from '@mui/material/TableHead';

const TableHead = (props: TableHeadProps) => <TableHeadMui {...props} />;

export { TableHead };
export default TableHead;
export * from '@mui/material/TableHead';
