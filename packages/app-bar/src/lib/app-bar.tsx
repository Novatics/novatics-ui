import AppBarMui, { AppBarProps } from '@mui/material/AppBar';

const AppBar = (props: AppBarProps) => <AppBarMui {...props} />;

export { AppBar };
export default AppBar;
export * from '@mui/material/AppBar';
