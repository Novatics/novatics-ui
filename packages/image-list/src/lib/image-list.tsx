import ImageListMui, { ImageListProps } from '@mui/material/ImageList';

const ImageList = (props: ImageListProps) => <ImageListMui {...props} />;

export { ImageList };
export default ImageList;
export * from '@mui/material/ImageList';
