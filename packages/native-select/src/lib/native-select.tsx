import NativeSelectMui, { NativeSelectProps } from '@mui/material/NativeSelect';

const NativeSelect = (props: NativeSelectProps) => (
  <NativeSelectMui {...props} />
);

export { NativeSelect };
export default NativeSelect;
export * from '@mui/material/NativeSelect';
