import BackdropMui, { BackdropProps } from '@mui/material/Backdrop';

const Backdrop = (props: BackdropProps) => <BackdropMui {...props} />;

export { Backdrop };
export default Backdrop;
export * from '@mui/material/Backdrop';
