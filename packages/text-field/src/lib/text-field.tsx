import TextFieldMui, { TextFieldProps } from '@mui/material/TextField';

const TextField = (props: TextFieldProps) => <TextFieldMui {...props} />;

export { TextField };
export default TextField;
export * from '@mui/material/TextField';
