import CheckboxMui, { CheckboxProps } from '@mui/material/Checkbox';

const Checkbox = (props: CheckboxProps) => <CheckboxMui {...props} />;

export { Checkbox };
export default Checkbox;
export * from '@mui/material/Checkbox';
