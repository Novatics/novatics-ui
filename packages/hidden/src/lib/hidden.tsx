import HiddenMui, { HiddenProps } from '@mui/material/Hidden';

const Hidden = (props: HiddenProps) => <HiddenMui {...props} />;

export { Hidden };
export default Hidden;
export * from '@mui/material/Hidden';
