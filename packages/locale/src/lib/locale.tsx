import LocaleMui, { LocaleProps } from '@mui/material/Locale';

const Locale = (props: LocaleProps) => <LocaleMui {...props} />;

export { Locale };
export default Locale;
export * from '@mui/material/Locale';
