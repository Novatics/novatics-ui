import DividerMui, { DividerProps } from '@mui/material/Divider';

const Divider = (props: DividerProps) => <DividerMui {...props} />;

export { Divider };
export default Divider;
export * from '@mui/material/Divider';
