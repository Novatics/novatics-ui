import ListItemMui, { ListItemProps } from '@mui/material/ListItem';

const ListItem = (props: ListItemProps) => <ListItemMui {...props} />;

export { ListItem };
export default ListItem;
export * from '@mui/material/ListItem';
