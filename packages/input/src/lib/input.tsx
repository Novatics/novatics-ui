import InputMui, { InputProps } from '@mui/material/Input';

const Input = (props: InputProps) => <InputMui {...props} />;

export { Input };
export default Input;
export * from '@mui/material/Input';
