import TableMui, { TableProps } from '@mui/material/Table';

const Table = (props: TableProps) => <TableMui {...props} />;

export { Table };
export default Table;
export * from '@mui/material/Table';
