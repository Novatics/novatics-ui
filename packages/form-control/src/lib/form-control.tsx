import FormControlMui, { FormControlProps } from '@mui/material/FormControl';

const FormControl = (props: FormControlProps) => <FormControlMui {...props} />;

export { FormControl };
export default FormControl;
export * from '@mui/material/FormControl';
